{
  "name": "bivia-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "browser-sync start --server --files \"*.html, styles/*.css, components/*.js, components/*.jsx\" --index BIVIA.html",
    "deploy": "vercel --prod"
  },
  "devDependencies": {
    "browser-sync": "^3.0.0"
  }
}
